# 心情守护 App — Figma 设计规范（修订版）

> 本文为视觉与交互设计规范，供 Figma 设计使用。当前阶段只更新设计规范，不代表已经执行前端代码修改。

## 一、设计目标

### 1. 风格定位

- 温柔、治愈、有呼吸感。
- 使用低饱和、柔和、圆润的莫兰迪色系。
- 避免高饱和荧光色、强烈渐变和过度装饰。
- 重点信息清晰，页面留白充足，降低情绪记录的压力。

### 2. 布局原则

- 页面内容整体居中。
- 桌面端主要内容最大宽度建议为 `1080px`。
- 日记表单、推荐内容等阅读区域建议限制在 `640px–720px`，避免单行过长。
- 统计图表、历史记录等信息密度较高的页面可使用完整的 `1080px` 内容宽度。
- 移动端使用单列布局，页面左右安全边距不小于 `16px`。

### 3. 交互原则

- 所有可点击元素必须有默认、hover、active、focus、disabled、loading 状态。
- 弹窗、Tab、按钮和主题切换使用 `0.2s ease` 过渡。
- 删除、退出登录等危险操作必须二次确认。
- 空状态、加载状态、错误状态和成功状态必须有明确的文字反馈。
- 不仅依赖颜色传达信息，同时使用文字、图标、边框或形状区分状态。

## 二、主题机制

- 使用 CSS 变量驱动全局主题。
- 支持 5 套莫兰迪主题切换。
- 默认主题为鼠尾草绿 `sage-green`。
- 主题切换后，页面背景、卡片、文字、边框、按钮、标签、图表、日程分类色和弹窗等同步更新。
- 原“雾霾蓝 `haze-blue`”主题更名为“梦幻蓝 `dreamy-blue`”。
- Figma 中建立一套颜色变量，并创建 5 套主题变量组；切换主题时只切换变量映射。

### 1. 默认主题：鼠尾草绿 `sage-green`

| Token | 色值 | 用途 |
|---|---|---|
| `--app-bg` | `#F3F4F0` | 页面背景 |
| `--app-card-bg` | `#FFFFFF` | 卡片、弹窗背景 |
| `--app-primary` | `#A8B5A0` | 主按钮、高亮、选中状态 |
| `--app-primary-light` | `#C5CFBE` | hover、浅色高亮、描边 |
| `--app-text` | `#4A5248` | 标题、正文、主要信息 |
| `--app-text-light` | `#8B9488` | 说明、时间、占位文字 |
| `--app-border` | `#E3E6E0` | 边框、分割线 |
| `--app-success` | `#B8D0B0` | 高心情、完成、成功 |
| `--app-danger` | `#D4A8A0` | 低心情、错误、删除 |
| `--app-warning` | `#DCC89E` | 提醒、中等心情、待反馈 |
| `--app-cat-study` | `#D6E0D0` | 日程-学习 |
| `--app-cat-work` | `#E0E8D8` | 日程-工作 |
| `--app-cat-life` | `#F0E8D0` | 日程-生活 |

### 2. 梦幻蓝 `dreamy-blue`

| Token | 色值 | 用途 |
|---|---|---|
| `--app-bg` | `#F5F5F8` | 页面背景 |
| `--app-card-bg` | `#FFFFFF` | 卡片、弹窗背景 |
| `--app-primary` | `#99A4BC` | 主按钮、高亮、选中状态 |
| `--app-primary-light` | `#B8C0D4` | hover、浅色高亮、描边 |
| `--app-text` | `#4A5260` | 标题、正文、主要信息 |
| `--app-text-light` | `#8A93A3` | 说明、时间、占位文字 |
| `--app-border` | `#E3E5EB` | 边框、分割线 |
| `--app-success` | `#B0C8B8` | 高心情、完成、成功 |
| `--app-danger` | `#D0A8B0` | 低心情、错误、删除 |
| `--app-warning` | `#D8C498` | 提醒、中等心情、待反馈 |
| `--app-cat-study` | `#D6DEEC` | 日程-学习 |
| `--app-cat-work` | `#DCE4DC` | 日程-工作 |
| `--app-cat-life` | `#ECE4D6` | 日程-生活 |

### 3. 其他主题

| 主题 | 主色 `--app-primary` | 背景 `--app-bg` | 主文字 `--app-text` | 主色浅 `--app-primary-light` |
|---|---|---|---|---|
| 灰粉 `grey-pink` | `#D4A5A5` | `#F7F2F2` | `#5A4E4E` | `#E0C4C4` |
| 灰紫 `grey-purple` | `#B5A8C0` | `#F5F3F7` | `#4E4858` | `#D0C5D8` |
| 暖杏 `warm-apricot` | `#D4B896` | `#F8F4EF` | `#5A4E40` | `#E0CDB5` |

其他三套主题也必须补充完整的 `--app-card-bg`、`--app-text-light`、`--app-border`、`--app-success`、`--app-danger`、`--app-warning`、`--app-cat-study`、`--app-cat-work`、`--app-cat-life`，并保证与主题主色协调。

## 三、颜色使用规则

- `--app-bg`：页面整体背景，不用于主要文字。
- `--app-card-bg`：卡片、弹窗、表单容器背景。
- `--app-primary`：主按钮、链接、Tab 选中态、重点操作和重要数据。
- `--app-primary-light`：hover 背景、浅色高亮、输入框聚焦辅助色和次级描边。
- `--app-text`：页面标题、正文、主要信息。
- `--app-text-light`：辅助说明、时间、占位文字和次要信息。
- `--app-border`：卡片、输入框、分割线和控件边框。
- `--app-success`：完成、高心情、成功提示和正向反馈。
- `--app-danger`：低心情、错误、删除和危险操作。
- `--app-warning`：提醒、中等心情、待反馈和需要处理的状态。
- `--app-cat-study`、`--app-cat-work`、`--app-cat-life`：对应日程分类背景及分类标识。
- 日程分类颜色需要根据不同主题单独配置，背景与左侧色条保持同色系但有足够层次。
- 不得只依赖颜色区分状态；应同步显示状态文字、图标、边框或删除线。

## 四、Element Plus 主题适配规范

所有 Element Plus 组件必须跟随当前主题变量，不得保留与主题冲突的固定蓝色或固定灰色。需要覆盖：

- Button：主按钮、次按钮、文字按钮、危险按钮、hover、active、disabled、loading。
- Tag：默认、成功、警告、危险、信息及选中状态。
- Tabs、Radio Button：选中、hover、未选中和焦点状态。
- Input、Select、Cascader、Date Picker：背景、边框、文字、占位、聚焦和禁用状态。
- Rate：未选中、hover 和选中颜色。
- Dialog、Message Box：背景、标题、正文、边框、遮罩和关闭按钮。
- Message、Alert：成功、警告、错误、信息状态。
- Progress、Empty、Descriptions、Loading：颜色、背景和辅助文字。

## 五、字体规范

### 字体族

- 中文正文：`"PingFang SC", "Microsoft YaHei", "Hiragino Sans GB", sans-serif`
- 数字与评分：`"DIN Alternate", "Georgia", sans-serif`

### 字号与字重

| 层级 | 字号 | 字重 | 行高 | 用途 |
|---|---:|---:|---:|---|
| H1 | 24px | 600 | 32px | 页面大标题 |
| H2 | 20px | 600 | 28px | 区块标题 |
| H3 | 16px | 600 | 24px | 卡片标题 |
| Body | 14px | 400 | 22px | 正文 |
| Caption | 12px | 400 | 18px | 说明、时间戳 |
| Small | 11px | 400 | 16px | 标签、角标 |

## 六、间距、圆角与阴影

采用 4px 基准：

| Token | 值 | 用途 |
|---|---:|---|
| `space-1` | 4px | 图标与文字 |
| `space-2` | 8px | 组件内部间距 |
| `space-3` | 12px | 表单元素间距 |
| `space-4` | 16px | 卡片内边距 |
| `space-5` | 24px | 区块间距 |
| `space-6` | 32px | 页面边距、分组间距 |
| `space-7` | 48px | 大分区间距 |

| Token | 值 | 用途 |
|---|---:|---|
| `radius-sm` | 8px | 小按钮、输入框 |
| `radius-md` | 12px | 标签、气泡、普通按钮 |
| `radius-lg` | 16px | 卡片、弹窗 |
| `radius-full` | 999px | 胶囊按钮、圆形色块 |

| Token | 值 | 用途 |
|---|---|---|
| `shadow-sm` | `0 2px 8px rgba(0,0,0,0.04)` | 卡片默认 |
| `shadow-md` | `0 4px 16px rgba(0,0,0,0.06)` | hover 卡片、弹窗 |
| `shadow-lg` | `0 8px 32px rgba(0,0,0,0.08)` | 模态框、重要提示 |

## 七、全局导航

- 左侧或顶部导航包含：心情日记、日程管理、治愈助手、我的片单/书单。
- 右上角保留当前代码的独立“个人设置”入口，不设计弹出面板、Popover 或抽屉。
- 点击“个人设置”进入独立设置页面 `/settings`。
- 导航栏使用白色半透明磨砂背景，当前路由使用主题色下划线或胶囊背景高亮。
- 导航可点击区域必须有 hover、active 和键盘 focus 状态。

## 八、页面设计规范

### 1. 登录 / 注册页 `/login`

- 桌面端采用左右分栏：品牌区约 40%，表单区约 60%。
- 品牌区使用当前主题的浅色渐变至页面背景，不使用固定紫蓝渐变。
- 品牌文案：
  - “记录心情，也记录生活。”
  - “心情守护”
- 表单区包含“欢迎回来”或“创建账号”、用户名、密码、提交按钮和登录/注册切换入口。
- 表单使用圆角卡片、柔和阴影和主题色主按钮。
- 输入框包含默认、聚焦、错误和禁用状态。
- 右下角可放置 5 个主题色小圆点，点击后即时切换主题。
- 移动端改为上下布局，品牌区缩短，表单区优先展示。
- 页面整体重点是简洁、美观、安静，不增加复杂插画和过度装饰。

### 2. 心情日记页 `/diary`

- 统一使用路由 `/diary`。
- 页面顶部使用三个 Tab：写日记、历史日志、心情报告。
- 历史日志和心情报告不使用独立顶部导航入口。

#### 写日记

- 展示日期选择、心情评分、正文输入、AI 分析、标签选择和保存按钮。
- 心情评分使用 40px 大号星星，支持默认、hover、选中和禁用状态。
- 星星下方实时显示分值和对应心情文案。
- 正文输入框位于卡片中，右下角显示字数统计。
- AI 分析按钮使用主题色，可有轻微主题渐变；加载时显示“正在感受你的心情……”。
- AI 分析后展示情绪标签和事件标签，标签为圆润胶囊样式，可删除和手动添加。

#### 历史日志

- 顶部提供日期范围筛选，默认最近 30 天。
- 日志按日期分组，组头显示日期、当日平均分和日志条数。
- 日志卡片左侧使用 4px 心情色条：高心情使用 `--app-success`，低心情使用 `--app-danger`，中间状态使用 `--app-warning`。
- 卡片显示心情分、标签和正文摘要。
- 点击日志卡片打开详情/编辑弹窗。
- 编辑弹窗支持修改日期、心情分数、正文、情绪标签和事件标签，并保存更新。
- 保留“查看心情图表”按钮，点击进入独立统计分析页面。

#### 心情报告

- 提供周报/月报切换和周期前后导航。
- 展示平均心情、最好的一天、最低落的一天、日志数量、日程完成率和主要标签等摘要信息。
- 不嵌入 `Stats.vue` 的统计图表。
- 不保留标签影响分析模块。
- 无数据时显示“该周期暂无记录”。

### 3. 统计分析页 `/stats`

- 保留独立统计分析页面。
- 由历史日志中的“查看心情图表”按钮进入。
- 展示日、周、月的每日平均心情柱状图。
- 柱形图根据分数使用主题化颜色：高分使用 `--app-success`，低分使用 `--app-danger`，中等分数使用 `--app-warning` 或 `--app-primary-light`。
- 不包含标签影响分析。

### 4. 日程管理页 `/schedule`

- 顶部提供日/周视图切换、日期导航、添加日程、治愈一下和一键完成。
- 日视图展示 6:00–23:00 时间轴和单列日程。
- 周视图展示时间轴与七列日期。
- 日程块使用 `radius-sm`，左侧显示分类色条，内容包含标题和起止时间。
- 分类颜色必须使用当前主题对应的 `--app-cat-study`、`--app-cat-work`、`--app-cat-life`。
- 完成后的日程降低饱和度或透明度，并增加删除线；同时保留完成状态文字或图标。
- 点击日程块打开编辑弹窗；点击小圆框切换完成状态。
- 时间轴支持自然滚动。

### 5. 治愈助手页 `/heal`

- 页面顶部使用两个 Tab：获取建议、治愈历史。
- 两个 Tab 共用 `/heal` 路由。

#### 获取建议

- 中央主按钮文案为“帮我治愈一下”。
- 主按钮使用当前主题主色，并增加呼吸光晕动画；动画应克制、柔和，不造成视觉干扰。
- 推荐结果卡片展示活动类型、建议文案、建议时长和建议时间段。
- 四类活动使用图标和文字同时表达：
  - 电影：电影图标
  - 读书：书籍图标
  - 运动：运动图标
  - 放松：冥想或放松图标
- 若没有具体时间安排，时长显示“无需额外时间”。
- 按钮行包含“就它了”和“换一个”。
- 推荐卡片使用渐变边框，渐变颜色根据当前主题主色及 `--app-primary-light` 自动变化。
- 卡片内文字、背景、边框和按钮均不得使用固定主题色。

#### 治愈历史

- 使用列表或时间线展示历史推荐。
- 筛选条件严格为：
  - 全部
  - 电影
  - 读书
  - 运动
  - 放松
  - 待反馈
- 不设计“未采纳”“已采纳”“已反馈”筛选项。
- 每条记录展示建议内容、活动类型、时长、建议时间段、状态、评分和感想（有数据时）。
- “待反馈”状态使用 `--app-warning`，并配合文字或图标表达，不能只依赖颜色。

### 6. 个人设置页 `/settings`

- 保持当前代码的独立页面形态。
- 不设计右上角弹出面板、Popover 或抽屉。
- 页面包含：基本信息、主题色、治愈偏好、保存偏好和退出登录。
- 主题色区域展示 5 个圆形色块，当前选中主题显示深色外圈和对勾。
- 治愈偏好包含电影类型、书籍类型和运动方式。
- 保存按钮使用当前主题主色。

### 7. 我的片单/书单页 `/library`

- 顶部两个 Tab：历史电影、在读书目。
- 保持当前代码的纵向列表结构。
- 不设计电影/书籍 emoji 封面。
- 不要求卡片网格布局。
- 不要求展示加入时间。
- 不要求阅读进度条。
- 不额外强化空状态视觉，只保留必要的空状态文字。
- 书籍支持“在读”“已读”状态、“已读完”操作和“查看评价”弹窗。

## 九、状态与响应式规范

### 1. 通用状态

所有主要组件至少设计：

- 默认
- hover
- active
- focus
- disabled
- loading
- error
- empty
- success

### 2. 响应式断点

- 桌面端：`≥ 1024px`
- 平板端：`768px–1023px`
- 移动端：`< 768px`
- 窄屏手机：`< 480px`

需要明确：

- 导航在窄屏时的换行方式。
- 日程周视图在移动端允许横向滚动。
- 多列内容在移动端改为单列。
- 弹窗移动端宽度建议不超过 `92vw`。
- 设置页在移动端保持页面形式，不改为抽屉。

## 十、可访问性

- 主题色块必须有可读主题名称和可访问标签。
- 图标按钮必须有 `aria-label`。
- 键盘 Tab 可以访问导航、主题色块、Tab、按钮和表单控件。
- 文字与背景保持足够对比度。
- 颜色不能作为唯一状态标识。
- 删除、退出登录等危险操作必须提供二次确认。

## 十一、代码落地清单（供后续开发，不属于本次设计稿执行）

以下内容在设计规范确认后，再进行代码修改：

1. 增加统一的字体、圆角和阴影 CSS 变量。
2. 将默认主题、主题名称和五套主题颜色改为本规范定义的值。
3. 增加 `--app-warning`，并完善 Element Plus 全组件主题覆盖。
4. 将 `/write` 统一为 `/diary`。
5. 检查并补齐 `History.vue` 的日志点击编辑流程：打开弹窗、编辑日期、心情分数、正文、标签并保存更新。
6. 将 `HealHistory.vue` 筛选条件调整为“全部、电影、读书、运动、放松、待反馈”。
7. 登录页改为主题化、响应式的美观布局。
8. 为治愈助手增加四类活动图标、主按钮呼吸光晕和主题渐变边框。
9. 为每套主题分别配置协调的日程分类颜色。
10. 清理页面中与主题不一致的硬编码颜色，确保切换主题后页面颜色同步更新。
